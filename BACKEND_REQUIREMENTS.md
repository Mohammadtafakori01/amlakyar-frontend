# مستند نیازمندی‌های بک‌اند برای سیستم مدیریت فایل‌های ملکی

## بررسی تطبیق فرانت‌اند با مستند فنی

### ✅ موارد پیاده‌سازی شده در فرانت

1. **ماژول مدیریت فایل‌های ملکی**
   - ✅ زونکن املاک (OFFICE_MASTER)
   - ✅ زونکن تعاون داخلی (INTERNAL_COOPERATION)
   - ✅ زونکن تعاون خارجی (EXTERNAL_NETWORK)
   - ✅ دفتر فایل شخصی (PERSONAL)
   - ✅ CRUD کامل برای فایل‌ها
   - ✅ فیلتر و جستجو
   - ✅ Pagination

2. **فیلدهای فایل ملکی** (تمام فیلدهای مورد نیاز پیاده شده):
   - ✅ مالک (owner)
   - ✅ منطقه (region)
   - ✅ تلفن (phone)
   - ✅ تاریخ (date)
   - ✅ آدرس (address)
   - ✅ کد یکتا (uniqueCode)
   - ✅ نوع معامله (transactionType): فروش، اجاره، رهن، مشارکت، معاوضه
   - ✅ نوع ساختمان (buildingType): ویلا، آپارتمان، تجاری، بیرونی، قدیمی، اداری، مغازه
   - ✅ جهت (direction): شمالی، جنوبی، شرقی، غربی
   - ✅ طبقات (floors) با تمام جزئیات
   - ✅ واحد (unit)
   - ✅ قیمت کل/رهن (totalPrice)
   - ✅ قیمت متری/اجاره (unitPrice)
   - ✅ مساحت زمین (landArea)
   - ✅ تراکم (density)
   - ✅ طول (length)
   - ✅ عرض (width)
   - ✅ حیاط (yard)
   - ✅ حیاط خلوت (backyard)
   - ✅ زیرزمین (basement) - متراژ
   - ✅ سرویس مستخدم (servantRoom)
   - ✅ پاسیو (porch)
   - ✅ مسکونی (residential)
   - ✅ تخلیه (vacant)
   - ✅ اجاره (rentStatus)
   - ✅ سن بنا (buildingAge)
   - ✅ نما (facade)
   - ✅ منبع اطلاعاتی (informationSource)
   - ✅ محل سکونت مالک (ownerResidence)
   - ✅ وضعیت سند (documentStatus)
   - ✅ توضیحات (description)
   - ✅ تنظیم کننده (createdBy) - در response موجود است

3. **جزئیات طبقات** (تمام فیلدها پیاده شده):
   - ✅ عنوان طبقه (title)
   - ✅ شماره طبقه (floorNumber)
   - ✅ زیربنا (area)
   - ✅ خواب (bedrooms)
   - ✅ تلفن (phone)
   - ✅ آشپزخانه (kitchen)
   - ✅ آشپزخانه اوپن (openKitchen)
   - ✅ سرویس (bathroom)
   - ✅ کف پوش (flooring)
   - ✅ پارکینگ (parking)
   - ✅ انباری (storage)
   - ✅ شومینه (fireplace)
   - ✅ شوفاژ (heating)
   - ✅ کولر (cooler)
   - ✅ فن کوئل (fanCoil)
   - ✅ چیلر (chiller)
   - ✅ پکیج (package)
   - ✅ آسانسور (elevator)
   - ✅ سونا (sauna)
   - ✅ جکوزی (jacuzzi)
   - ✅ استخر (pool)
   - ✅ آیفون تصویری (videoIntercom)
   - ✅ درب ریموت (remoteDoor)

4. **ماژول دفترچه تلفن**
   - ✅ مدیریت مخاطبین املاک
   - ✅ مدیریت مخاطبین شخصی
   - ✅ Tab برای تفکیک

5. **ماژول ثبت مراجعات**
   - ✅ ثبت مراجعات مشتریان
   - ✅ نوع مراجعه (تلفنی/حضوری)
   - ✅ ثبت نیاز ملکی

6. **RBAC (Role-Based Access Control)**
   - ✅ دسترسی‌های مختلف بر اساس نقش
   - ✅ محدودیت‌های ایجاد/ویرایش/حذف

---

## ❌ موارد کم‌بوده در بک‌اند که باید پیاده‌سازی شوند

### 1. قابلیت تعاون از فایل شخصی به زونکن تعاون داخلی

**مشکل:** در مستند آمده که کاربر باید بتواند فایل شخصی خود را به "زونکن تعاون داخلی" منتقل کند، اما در فرانت فقط قابلیت به‌اشتراک‌گذاری از زونکن املاک وجود دارد.

**نیازمندی بک‌اند:**
```
POST /api/property-files/:id/share-from-personal
```

**توضیحات:**
- این endpoint باید فایل شخصی را به زونکن INTERNAL_COOPERATION منتقل کند
- فقط مالک فایل (createdBy) می‌تواند این کار را انجام دهد
- فایل در زونکن PERSONAL باقی می‌ماند و یک کپی در INTERNAL_COOPERATION ایجاد می‌شود
- یا فایل از PERSONAL حذف شده و به INTERNAL_COOPERATION منتقل می‌شود (بسته به منطق کسب‌وکار)

**RBAC:**
- ✅ ADMIN: می‌تواند فایل‌های شخصی خود را به تعاون منتقل کند
- ✅ SUPERVISOR: می‌تواند فایل‌های شخصی خود را به تعاون منتقل کند
- ✅ CONSULTANT: می‌تواند فایل‌های شخصی خود را به تعاون منتقل کند
- ❌ SECRETARY: دسترسی ندارد (فایل شخصی ندارد)

### 2. فیلد "اطلاعی" (مشکوک)

**مشکل:** در لیست فیلدهای کاربر، "اطلاعی" ذکر شده که نامشخص است.

**پیشنهاد:**
- اگر منظور "اطلاعاتی" است، همان `informationSource` است که پیاده شده
- اگر فیلد جداگانه‌ای است، باید مشخص شود چیست

**نیازمندی بک‌اند:**
- در صورت نیاز، فیلد جدید اضافه شود

### 3. بهبود به‌اشتراک‌گذاری خارجی ✅

**وضعیت:** ✅ پیاده‌سازی شده در فرانت و بک‌اند

#### 3.1. به‌اشتراک‌گذاری فایل شخصی به تعاون خارجی (برای سرپرست) ✅

**وضعیت:** ✅ پیاده‌سازی شده در بک‌اند

**Endpoint:**
```
POST /api/property-files/:id/share-from-personal-to-external
```

**Request Body:**
```json
{
  "targetEstateId": "string" // اجباری
}
```

**توضیحات:**
- ✅ این endpoint فایل شخصی را به زونکن EXTERNAL_NETWORK منتقل می‌کند
- ✅ فقط مالک فایل (createdBy) می‌تواند این کار را انجام دهد
- ✅ فقط سرپرست (SUPERVISOR) می‌تواند فایل‌های شخصی خود را به تعاون خارجی به‌اشتراک بگذارد
- ✅ بررسی مالکیت فایل شخصی انجام می‌شود
- ✅ ایجاد کپی در EXTERNAL_NETWORK
- ✅ جلوگیری از به‌اشتراک‌گذاری با آژانس خود

**RBAC:**
- ✅ SUPERVISOR: می‌تواند فایل‌های شخصی خود را به تعاون خارجی به‌اشتراک بگذارد
- ❌ سایر نقش‌ها: دسترسی ندارند

#### 3.2. به‌اشتراک‌گذاری فایل‌های مختلف به تعاون خارجی (برای مدیر) ✅

**وضعیت:** ✅ پیاده‌سازی شده در بک‌اند

**Endpoint:**
```
POST /api/property-files/:id/share-external
```

**Request Body:**
```json
{
  "targetEstateId": "string" // اجباری
}
```

**پشتیبانی از فایل‌های مختلف:**
- ✅ OFFICE_MASTER (قبلاً موجود بود)
- ✅ INTERNAL_COOPERATION (پیاده‌سازی شده)
- ✅ PERSONAL (فقط فایل‌های خود مدیر - پیاده‌سازی شده)

**توضیحات:**
- ✅ مدیر می‌تواند فایل‌های OFFICE_MASTER، INTERNAL_COOPERATION و PERSONAL خود را به تعاون خارجی به‌اشتراک بگذارد
- ✅ برای فایل‌های PERSONAL، فقط فایل‌های خود مدیر (createdBy) قابل به‌اشتراک‌گذاری هستند
- ✅ بررسی دسترسی و مالکیت انجام می‌شود
- ✅ جلوگیری از به‌اشتراک‌گذاری با آژانس خود

**RBAC:**
- ✅ ADMIN: می‌تواند فایل‌های OFFICE_MASTER، INTERNAL_COOPERATION و PERSONAL خود را به تعاون خارجی به‌اشتراک بگذارد
- ❌ سایر نقش‌ها: دسترسی ندارند

**نکات پیاده‌سازی شده:**
- ✅ validation انجام می‌شود که فایل در zone مناسب باشد
- ✅ بررسی می‌شود که کاربر دسترسی به فایل دارد
- ✅ برای فایل‌های PERSONAL، بررسی می‌شود که createdBy همان کاربر فعلی است

### 4. فیلتر پیشرفته‌تر ✅

**وضعیت:** ✅ پیاده‌سازی شده در بک‌اند

**Endpoint:**
```
GET /api/property-files?filters...
```

**فیلترهای پیاده‌سازی شده:**
- ✅ فیلتر بر اساس zone (zone)
- ✅ فیلتر بر اساس نوع معامله (transactionType)
- ✅ فیلتر بر اساس نوع ساختمان (buildingType)
- ✅ فیلتر بر اساس محدوده قیمت (minPrice, maxPrice)
- ✅ فیلتر بر اساس محدوده مساحت (minArea, maxArea)
- ✅ فیلتر بر اساس تاریخ (fromDate, toDate)
- ✅ فیلتر بر اساس منطقه (region)
- ✅ جستجو در چند فیلد (search)

### 5. Audit Logs برای فایل‌ها ✅

**وضعیت:** ✅ پیاده‌سازی شده در بک‌اند

**Endpoint:**
```
GET /api/property-files/:id/audit-logs
```

**قابلیت‌های پیاده‌سازی شده:**
- ✅ ثبت تاریخچه تغییرات هر فایل
- ✅ ثبت چه کسی، چه زمانی، چه تغییری داده است
- ✅ نمایش تاریخچه در response

**پیشنهاد ساختار:**
```typescript
interface PropertyFileAuditLog {
  id: string;
  propertyFileId: string;
  action: 'CREATE' | 'UPDATE' | 'DELETE' | 'SHARE_INTERNAL' | 'SHARE_EXTERNAL';
  changedBy: {
    id: string;
    firstName: string;
    lastName: string;
  };
  changes?: {
    field: string;
    oldValue: any;
    newValue: any;
  }[];
  timestamp: string;
}
```

### 6. Validation و Business Rules

**نیازمندی بک‌اند:**

1. **کد یکتا (uniqueCode):**
   - باید در هر estate یکتا باشد
   - یا در کل سیستم یکتا باشد (بسته به منطق کسب‌وکار)

2. **قوانین به‌اشتراک‌گذاری:**
   - فایل‌های EXTERNAL_NETWORK نباید قابل ویرایش باشند
   - فایل‌های INTERNAL_COOPERATION فقط خواندنی هستند
   - فایل‌های PERSONAL فقط برای مالک قابل ویرایش

3. **Validation فیلدها:**
   - شماره تلفن: 11 رقم، شروع با 09
   - تاریخ: فرمت YYYY-MM-DD
   - قیمت: عدد مثبت
   - مساحت: عدد مثبت

### 7. Soft Delete برای فایل‌ها ✅

**وضعیت:** ✅ پیاده‌سازی شده در بک‌اند

**قابلیت‌های پیاده‌سازی شده:**
- ✅ فایل‌ها از دیتابیس حذف نمی‌شوند (soft delete)
- ✅ فیلد `deletedAt` موجود است
- ✅ فایل‌های حذف شده در لیست نمایش داده نمی‌شوند
- ✅ امکان بازیابی فایل‌های حذف شده (برای ADMIN)
- ✅ Endpoint بازیابی: `POST /api/property-files/:id/restore`

### 8. Bulk Operations ✅

**وضعیت:** ✅ پیاده‌سازی شده در بک‌اند

**Endpoint:**
```
POST /api/property-files/bulk-operations
```

**Request Body:**
```json
{
  "fileIds": ["uuid1", "uuid2", ...],
  "operation": "DELETE" | "SHARE_INTERNAL",
  "targetEstateId": "uuid" // برای SHARE_INTERNAL
}
```

### 9. Export/Import

**نیازمندی بک‌اند:**
```
GET /api/property-files/export?format=excel&filters=...
POST /api/property-files/import
```

### 10. جستجوی پیشرفته

**نیازمندی بک‌اند:**
- جستجو در تمام فیلدهای متنی
- جستجوی فازی (Fuzzy Search)
- جستجو با فیلترهای ترکیبی

### 11. Statistics و Reports ✅

**وضعیت:** ✅ پیاده‌سازی شده در بک‌اند

**Endpoint:**
```
GET /api/property-files/statistics
```

**آمارهای پیاده‌سازی شده:**
- ✅ تعداد فایل‌ها بر اساس zone
- ✅ تعداد فایل‌ها بر اساس نوع معامله
- ✅ تعداد فایل‌ها بر اساس نوع ساختمان
- ✅ میانگین قیمت
- ✅ تعداد فایل‌های اخیر (آخرین 30 روز)

### 12. Notifications

**نیازمندی بک‌اند:**
- اطلاع‌رسانی هنگام به‌اشتراک‌گذاری فایل
- اطلاع‌رسانی هنگام ایجاد فایل جدید در تعاون
- WebSocket یا Server-Sent Events برای اعلان‌های Real-time

### 13. فیلدهای اضافی پیشنهادی ✅

**وضعیت:** ✅ پیاده‌سازی شده در بک‌اند

**فیلدهای اضافی پیاده‌سازی شده:**
- ✅ `tags`: برچسب‌ها برای دسته‌بندی بهتر
- ✅ `priority`: اولویت فایل (HIGH, MEDIUM, LOW)
- ✅ `status`: وضعیت فایل (ACTIVE, INACTIVE, PENDING)
- ✅ `expiryDate`: تاریخ انقضا (برای فایل‌های موقت)
- ✅ `attachments`: فایل‌های ضمیمه (تصاویر، اسناد)

---

## 📊 خلاصه وضعیت پیاده‌سازی

### ✅ موارد پیاده‌سازی شده در بک‌اند

1. **به‌اشتراک‌گذاری فایل شخصی به تعاون خارجی (برای سرپرست)** ✅
   - Endpoint: `POST /api/property-files/:id/share-from-personal-to-external`
   - بررسی مالکیت و دسترسی
   - جلوگیری از به‌اشتراک‌گذاری با آژانس خود

2. **بهبود به‌اشتراک‌گذاری خارجی برای مدیر** ✅
   - پشتیبانی از OFFICE_MASTER، INTERNAL_COOPERATION و PERSONAL
   - بررسی دسترسی و مالکیت
   - جلوگیری از به‌اشتراک‌گذاری با آژانس خود

3. **Validation و Business Rules** ✅
   - بهبود بررسی یکتایی uniqueCode
   - Validation کامل فیلدها
   - targetEstateId اجباری

4. **فیلترهای پیشرفته** ✅
   - فیلتر بر اساس zone، transactionType، buildingType
   - فیلتر قیمت، مساحت، تاریخ و منطقه
   - جستجو در چند فیلد

5. **Audit Logs** ✅
   - `GET /api/property-files/:id/audit-logs`

6. **Soft Delete** ✅
   - با امکان بازیابی (`POST /api/property-files/:id/restore`)

7. **Bulk Operations** ✅
   - `POST /api/property-files/bulk-operations`

8. **Statistics** ✅
   - `GET /api/property-files/statistics`

9. **فیلدهای اضافی** ✅
   - tags, priority, status, expiryDate, attachments

### ⏳ موارد باقی‌مانده (اولویت پایین)

1. **Export/Import** - نیاز به پیاده‌سازی
2. **Notifications** - نیاز به پیاده‌سازی (WebSocket/SSE)
3. **فیلد "اطلاعی"** - نیاز به توضیح بیشتر

---

## خلاصه اولویت‌ها

### اولویت بالا (Critical) ✅
1. ✅ قابلیت تعاون از فایل شخصی به زونکن تعاون داخلی
2. ✅ Validation و Business Rules
3. ✅ Soft Delete
4. ✅ به‌اشتراک‌گذاری فایل شخصی به تعاون خارجی (برای سرپرست)
5. ✅ بهبود به‌اشتراک‌گذاری خارجی برای مدیر

### اولویت متوسط (Important) ✅
6. ✅ Audit Logs
7. ✅ فیلتر پیشرفته‌تر
8. ✅ جستجوی پیشرفته (جستجو در چند فیلد)

### اولویت پایین (Nice to Have)
9. ✅ Bulk Operations
10. ⏳ Export/Import (نیاز به پیاده‌سازی)
11. ✅ Statistics و Reports
12. ⏳ Notifications (نیاز به پیاده‌سازی - WebSocket/SSE)
13. ✅ فیلدهای اضافی

---

## نکات مهم برای تیم بک‌اند

1. **Data Isolation:** فایل‌های شخصی مشاوران باید کاملاً ایزوله باشند
2. **Performance:** با توجه به حجم داده‌ها، pagination و indexing مهم است
3. **Security:** تمام endpoint ها باید RBAC داشته باشند
4. **API Consistency:** تمام response ها باید ساختار یکسانی داشته باشند
5. **Error Handling:** پیام‌های خطای واضح و مفید

